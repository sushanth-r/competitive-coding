from typing import List


class Solution:
    def countBits(self, n: int) -> List[int]:
        result = []
        for i in range(n + 1):
            temp = i
        return result


class CountBits:
    n = int(input())
    x = Solution().countBits(n)
    print(x)
